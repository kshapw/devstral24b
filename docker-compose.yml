version: "3.8"

services:
  app:
    build: .
    ports:
      - "2024:2024"
    environment:
      - ENVIRONMENT=docker
    volumes:
      - chat_data:/app/data
    depends_on:
      - qdrant
      - ollama
    healthcheck:
      test: [ "CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:2024/')" ]
      interval: 10s
      timeout: 5s
      retries: 3
      start_period: 10s
    restart: unless-stopped

  qdrant:
    image: qdrant/qdrant
    ports:
      - "127.0.0.1:6343:6333"
    volumes:
      - qdrant_storage:/qdrant/storage
    restart: unless-stopped

  ollama:
    image: ollama/ollama
    ports:
      - "127.0.0.1:11441:11434"
    volumes:
      - ollama_models:/root/.ollama
    environment:
      - OLLAMA_KEEP_ALIVE=-1
    restart: unless-stopped
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [ gpu ]

volumes:
  qdrant_storage:
  ollama_models:
  chat_data:
